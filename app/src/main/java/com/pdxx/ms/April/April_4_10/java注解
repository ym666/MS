java 注解
在编译期间可以检查
元注解 @target @retation 等
参数 必须有初始值

java 反射
可以得到任何一个类的构造 方法 变量
Class类
newInstance
invoke

java泛型
java1.5引入的新特性，在定义类跟接口时，可以使用类型参数，在实际使用的时候用实际的类型替换。（类似于形参跟实参）
类型擦除
在生成java字节码的时候，会把类型参数给去掉。例如list<String>和List<Integer> 都会变成List
静态变量是被泛型类的所有实例共享的
泛型的类型参数不能用在runTime的时候，比如异常处理，类型擦除后无法分辨

